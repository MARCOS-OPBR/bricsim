[project]
name = "bricsim-simulator"
version = "0.1.0"
description = "BRICSim engine (core/dsl/objects/ui)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
[project.optional-dependencies]
ui = ["PyQt5"]
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
where = ["."]
